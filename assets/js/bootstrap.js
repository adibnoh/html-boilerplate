window.$ = window.jQuery = require('jquery');

window.sortable = require('sortablejs');

window.slug = require('slugify');

window.datepicker = require('flatpickr');

window.moment = require('fecha');